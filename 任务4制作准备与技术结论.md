# 任务4制作总结与技术结论

## 0. 文档状态

这份文档最初用于第四关制作准备。第四关现已完成到 Phase 8，因此本文改为记录最终实现、作废方案和已经通过实战验证的制作技巧。

详细剧情与数值见 [任务4同舟共济.md](任务4同舟共济.md)，通用方法论见 [战役设计方法与XML用法.md](战役设计方法与XML用法.md)。

---

## 1. 最终使用的文件

| 文件 | 第四关职责 |
| --- | --- |
| `maps/gatefall_scenario_04_galaxy_macro.xml` | 警惕凝视、异端的终结与星门快照 |
| `md/gatefall_scenario_04.xml` | 全部 Phase、AI、目标、失败、评分与 BGM 状态机 |
| `cutscenes/gatefall_s04_*.xml` | 开场、雷达船、M0、航母、平台、电站、信标和侦察机镜头 |
| `libraries/constructionplans.xml` | 玩家战术生产平台模块布局 |
| `libraries/loadouts.xml` | 玩家与固定友军舰船配装 |
| `libraries/gamestarts.xml` | 第四关入口、玩家船与星级门槛 |
| `libraries/sound_library.xml` | 第四关自定义音乐注册 |
| `t/0001-L086.xml` | 中文定稿 |
| `t/0001-L044.xml` | 英文定稿 |

第四关依赖 `Kingdom End`、`Cradle of Humanity`、`Split Vendetta` 与 `Timelines`。

---

## 2. 地图结论

### 2.1 星系相对位置

第四关使用两个单星区 Cluster：

```text
警惕凝视 Cluster 601  ── 远古跳跃门 ──  异端的终结 Cluster 31
(-150000000, 0, 103920000)              (-120000000, 0, 103920000)
```

警惕凝视位于异端的终结正西侧，`Δx=-30000000`、`Δz=0`。

使用的宏：

- `gatefall_scenario_04_galaxy_macro`
- `gatefall_scenario_04_cluster_601_macro`
- `gatefall_scenario_04_cluster_601_sector001_macro`
- `gatefall_scenario_04_cluster_31_macro`
- `gatefall_scenario_04_cluster_31_sector001_macro`

### 2.2 星门不能依赖主宇宙开门剧情

原版异端的终结与警惕凝视连接依赖 Boron 主线开门 cue。独立 Timelines galaxy 不会可靠继承该状态，因此第四关克隆所需 Cluster/Sector/Zone，并直接使用可工作的 `props_gates_anc_gate_macro`。

最终连接：

- 异端的终结：`connection_ClusterGate031To601b`
- 警惕凝视：`connection_ClusterGate601To031`

`031To601b` 末尾的 `b` 不能省略。

通往晨星、悲惨的天空和贫瘠海岸的其他星门仅保留为封锁布景。结尾 Xenon F 使用镜头与静默删除表现“朝 Argon 方向离开”，不要求当前双星区地图真的连接晨星。

### 2.3 环境必须显式复制

自定义宏不会自动命中原版 `mapdefaults`。本关显式复制了：

- 名称与环境。
- 日照参数。
- 资源区与音频区。
- 原有残骸和矿区连接。

异端的终结日照为 `1.81`，警惕凝视为 `0.41`。日照是 Sector 级参数，不存在“星区边缘天然日照更高”的局部区域；两座能源站与 M0 的关系必须由剧情和 MD 表现。

### 2.4 先确定构图，再写绝对坐标

第四关最终使用了两套清晰几何：

- 警惕凝视：残骸—星门约 45° 斜线，玩家与两组敌军沿斜线前后展开。
- 异端的终结：星门—M0 为主轴，雷达船、电站、造船设施和基地围绕主轴分布。

这种做法比反复“凭感觉加减 x/z”更稳定。先写出相对关系，再换算为 MD 的绝对坐标。

---

## 3. 固定舰船配装

第四关最终保留 8 套固定 loadout：

| 用途 | Loadout ID | 船体宏 |
| --- | --- | --- |
| 玩家九头蛇御用型 | `gatefall_scenario_04_hydra_regal_player` | `ship_bor_m_corvette_02_a_macro` |
| 碧蓝鳍长尾鲨 | `gatefall_scenario_04_thresher_anti_fighter` | `ship_bor_m_gunboat_01_a_macro` |
| 青紫鳍九头蛇 | `gatefall_scenario_04_hydra_anti_capital` | `ship_bor_m_corvette_01_a_macro` |
| 武士刀防空备用 | `gatefall_scenario_04_katana_anti_fighter` | `ship_ter_m_corvette_01_a_macro` |
| 武士刀介子流备用 | `gatefall_scenario_04_katana_meson` | `ship_ter_m_corvette_01_a_macro` |
| 青紫鳍镰刀 | `gatefall_scenario_04_scimitar_anti_capital` | `ship_ter_m_frigate_01_a_macro` |
| 巨鳐 | `gatefall_scenario_04_ray` | `ship_bor_l_destroyer_01_a_macro` |
| 席恩 | `gatefall_scenario_04_syn` | `ship_atf_l_destroyer_01_a_macro` |

### 3.1 席恩和札幌不能混用

- 席恩：`ship_atf_l_destroyer_01_a_macro`
- Timelines 札幌：`ship_ter_l_flagship_01_a_macro`

不要根据外形、级别或“Terran L 旗舰”猜宏名，应在原始 macro 和本地化页中交叉确认。

### 3.2 `_01_` 与 `_02_` 表面装备

- M 舰自身的集成 M 盾与虚拟炮塔通常使用 `_01_`。
- L/XL 舰体上的 M 表面盾与表面炮塔通常使用 `_02_`。

L 舰 loadout 必须按船体 group 写满表面位点，不能直接套用 M 舰的单 path 写法。

### 3.3 定义 loadout 不等于自动应用

固定友军可在 `<create_ship>` 中直接引用：

```xml
<loadout ref="gatefall_scenario_04_syn" />
```

依赖默认派系配装的 Xenon 或空间站模块，应显式指定 `faction`，必要时再次 `generate_loadout` 和 `apply_loadout`。仅写 `<level exact="1.0"/>` 在所有权暂存为 ownerless 时可能得到空炮塔或错误装备表。

---

## 4. 玩家战术生产平台

### 4.1 最终模块方案

`gatefall_scenario_04_forward_base` 包含：

- 2 个 Terran S/M 造船模块。
- 5 个 Terran L 造船模块，沿 X 轴排列并使用默认旋转。
- 5 个 Boron L 集装仓储模块。
- 4 个 Boron 圆盘防御模块。

造船模块与其他模块使用不同 Y 层，避免出口相互遮挡。

### 4.2 operational 不保证模块有武器

运行时直接创建完成状态空间站：

```xml
<create_station
  state="componentstate.operational"
  owner="faction.player"
  constructionplan="'gatefall_scenario_04_forward_base'" />
```

只代表模块已建成，不保证每个表面槽都有炮塔和护盾。第四关对每个模块执行：

```xml
<generate_loadout
  result="$ModuleLoadout"
  macro="$Station.macro"
  module="$Module.macro"
  faction="faction.boron"
  level="1.0"
  variation="0" />
<apply_loadout object="$Module" loadout="$ModuleLoadout.{1}" />
```

然后发送 `init station` 信号，确保四个防御模块真正武装。

同样的方法也用于两座 Xenon 电站、造船厂和船坞。

### 4.3 管理员、经销商和无人机要单独补

空间站生成后显式创建：

- `controlpost.manager`
- `controlpost.shiptrader`

无人机采用容量驱动，而不是写死总数量：

```xml
<add_units object="$ForwardBase" macro="macro.ship_gen_xs_repairdrone_01_a_macro"
           exact="[20, $ForwardBase.units.free].min" />
<add_units object="$ForwardBase" macro="macro.ship_gen_xs_buildingdrone_01_a_macro"
           exact="$ForwardBase.units.free" />
```

这样 construction plan 改变容量后仍能保持“20 个维修，其余全部建造”。

### 4.4 作废方案：不再使用关卡专用脚本配方

早期方案是“矿石交付 → MD 战术资源 → 脚本生成固定舰队包”，后来又尝试让 NPC 造船。这两种方案均已作废。

最终方案是：

- 战术生产平台归玩家。
- 使用真实 S/M 与 L 造船模块。
- Phase 6 授予本关需要的船体和装备蓝图。
- 使用异构石墨炔填补生产材料短缺。

因此本关不需要额外 `wares.xml` 配方，也不依赖 NPC 库存、NPC 蓝图或 NPC 造船逻辑。

---

## 5. 海豚与异构石墨炔

### 5.1 不要只统计脚本初始组

最初只遍历 `$Phase6SupportFreighters`，因此只有开局赠送的两艘海豚有效，玩家自己生产的海豚永远不会进入该组。

最终方案每分钟重新搜索：

```xml
<find_ship name="$DetectedDolphins"
           space="player.galaxy"
           trueowner="faction.player"
           macro="macro.ship_bor_m_trans_container_01_a_macro"
           multiple="true"
           recursive="true"
           checkoperational="true" />
```

然后按实际存在数量向玩家基地补充：

```text
min(空间站异构石墨炔剩余容量, 海豚数量 × 500)
```

这能覆盖：

- 初始赠送舰船。
- 玩家自己生产的舰船。
- 重新编组的舰船。
- 停靠在其他对象内部的舰船。

需要统计“玩家当前真正拥有的全部同型舰船”时，优先使用 `trueowner + recursive` 动态搜索，不要把出生组当作永久资产清单。

---

## 6. 定时刷怪

### 6.1 一次性时间点使用并列 cue

Phase 1 三分钟防守使用三个明确入口：

- 立即刷一轮。
- 60 秒 cue 刷第二轮。
- 120 秒 cue 刷第三轮。
- 180 秒 cue 收口。

不要让一个已经完成的 cue 尝试再次 signal 自己；这种“伪循环”常常只执行 0 秒和第一次延迟。

### 6.2 长期循环使用绝对时间戳

Phase 6 每五分钟刷船使用：

```text
$NextWaveTime = player.age + 300s
checkinterval = 5s
条件：player.age >= $NextWaveTime
触发后：立即把 $NextWaveTime 更新到下一次
```

这种结构比“cue 完成后重新 signal 自己”稳定，也容易在目标被摧毁时停止。

### 6.3 停止循环要做三层防护

摧毁造船厂/船坞时同时：

1. 设置 `$Destroyed=true`。
2. 把 `$NextWaveTime` 清零。
3. `cancel_cue` 周期 pulse。

此外，pulse 的 `<actions>` 内再检查一次目标存在和 destroyed flag。事件触发与五秒轮询可能在同一帧排队，仅在 `<conditions>` 检查不够安全。

---

## 7. 过场对象与无敌状态

### 7.1 ownerless 和 noattackresponse 会留下状态

将舰船设为 ownerless、等待并写入 `noattackresponse=true` 很适合过场，但恢复 Xenon 所有权并不保证飞行员黑板立即清空。常见后果：

- 舰船仍停火。
- 无法移动。
- 炮塔保持停火模式。
- 编队命令看似存在却不执行。

进入实战时至少执行：

```xml
<set_owner object="$Ship" faction="faction.xenon" overridenpc="true" />
<set_value name="$Ship.pilot.$noattackresponse" exact="false" />
<cancel_all_orders object="$Ship" />
```

然后重新创建实战命令，并在必要时把所有 operational turrets 设为 `weaponmode.attackenemies`。

### 7.2 最稳妥的办法是换掉过场道具

第一艘 SE 曾长期同时承担：

- 开局预创建对象。
- 过场镜头目标。
- ownerless 静止道具。
- 无敌目标。
- 最终可摧毁任务目标。

这些状态叠加后无法可靠解除。最终方案是在护卫清空时：

1. 静默删除旧 SE。
2. 在同一坐标创建新 Xenon SE。
3. 重新命名、加入目标组、设为可见。
4. 直接把新对象设为可摧毁任务目标。

如果关键目标的过场状态已经复杂到难以证明可逆，替换对象比继续叠加“解除状态”更稳。

### 7.3 无敌目标应拆成两段目标

雷达船流程采用：

1. 护卫组 destroy objective。
2. 护卫组清空。
3. 雷达船解除保护或替换。
4. 雷达船 destroy objective。

不要一开始就把无敌对象作为唯一摧毁目标，否则 UI 会诱导玩家攻击不会掉血的目标，也容易让阶段条件与对象状态互相等待。

---

## 8. 舰队 AI 与炮塔

### 8.1 指挥官关系必须配合正确 assignment

第四关静态防御统一使用：

- I、K：`assignment.bombardment`
- H、PE、M：`assignment.interception`

前者负责攻击大型目标，后者负责拦截小型舰船。单纯使用“保护指挥官”会让大型舰反应迟缓，或只围绕空间站移动。

### 8.2 重新设 commander 前先清理旧关系

所有权移交时应按顺序：

1. `cancel_all_orders`
2. `remove_object_commander`
3. `set_owner`
4. 新建 order 或新 commander/assignment

否则舰船可能已经属于 Boron，却仍持续跟随玩家或执行旧攻击命令。

### 8.3 默认配装也要指定派系

Xenon I 和 M0 护卫最终使用显式 Xenon 默认配装，并再次生成/应用 loadout。之后读取 `turrets.operational.list`，逐个设置为攻击敌人。

M0 本身的武器是宏内置子组件，不应为了“补武装”覆盖成普通生成 loadout。第四关保留 M0 的 thruster-only loadout，并在最终战显式激活宏内置炮塔。

---

## 9. 摄像头连续性

### 9.1 只平移仍可能在镜头接缝翻转 180°

两段 `camera_flyfromto` 即使使用相同 yaw，第二段的输入 orbit frame 也可能相对第一段输出 frame 翻转。第四关最终做法：

- 两段都固定 `orient_to="ecliptic"`。
- yaw 起止值保持一致，不在镜头内部插值角度。
- 第二段使用固定 180° frame 补偿。
- 接缝目标距离完全一致。

镜头看起来是连续平移，但底层参数不一定字面相同。应以游戏中的实际朝向为准。

### 9.2 镜头目标必须在拍摄期间冻结

碧蓝鳍队长机既是编队成员，又是第一段镜头终点。AI 队形会在镜头运行中移动它，因此第四关：

- 镜头开始前取消命令并等待。
- 两个 1 ms 帧后施加零线速度与零角速度。
- 第一段八秒结束后重新初始化 Wait，释放硬锁。

对于需要精准接缝的镜头，目标对象的位置稳定性与摄影机参数同样重要。

---

## 10. 动态对象的失败监听

打包船和战术生产平台在 Start 时尚不存在。若在 Start 子 cue 中直接写：

```xml
<event_object_destroyed object="$FutureObject" />
```

监听器可能在变量尚未绑定时注册到空对象，后续对象创建也不会自动修复。

第四关使用：

- `$ForwardBasePackerCreated`
- `$ForwardBaseCreated`
- `checkinterval="0.1s"`

条件同时检查创建 flag、场景状态、`.exists`、`.iswreck` 和 `hullpercentage`。

此外，真正需要被摧毁并触发失败的对象不能设置 `set_object_min_hull=1`。最低结构为 1 会使对象永远不会触发 destroyed；打捞船、打包船和基地均使用 0。

---

## 11. 空间站耐久与目标完成

### 11.1 MD 不能直接改最大结构

第四关要让四座 Xenon 战略站点只有约 10% 耐久。最终实现是：

```xml
<set_object_hull object="$Module" exact="10" />
<set_object_hull_unrepairable object="$Module" unrepairable="true" />
```

这不是修改最大结构，而是将每个模块预损伤到 10% 并禁止维修；对一次性 Timelines 战役足够稳定。

### 11.2 destroyed 事件后不要只等 `.exists=false`

空间站被摧毁后，对象引用可能在短时间内仍报告 exists。双电站流程使用显式布尔量：

- `$Phase6SolarADestroyed`
- `$Phase6SolarBDestroyed`

主线完成条件读取布尔量，而不是依赖对象引用何时消失。

### 11.3 倒计时超时必须保留正常完成路径

第二座电站的 120 秒不是失败条件。超时后应：

1. 移除带 endtime 的旧目标。
2. 建立无倒计时的新目标。
3. 生成惩罚增援。
4. 保留与正常路径相同的“双 destroyed flag”收口。

这样玩家超时后再摧毁电站仍能进入 Phase 8。

---

## 12. 动态 BGM

### 12.1 随机 sound 集合不是持续播放列表

在 `sound_library.xml` 中使用随机 sample 并设置无限 repeat，通常只在 `play_music` 调用时随机一次，随后无限循环同一首。第四关曾因此连续听到同一曲目。

最终做法：

- 每首曲子注册为独立 `repeat="1"` ID。
- MD 自己随机选择曲目。
- 记录曲目实际时长和 `$CurrentMusicEndTime`。
- 到时后重新 signal 选曲 cue。
- 抽取时排除上一首，避免连续重复。

### 12.2 战斗检测不能使用 `global.$PlayerShipsGroup`

该组主要覆盖玩家当前乘坐舰船及其停靠链，不代表玩家在 RTS 阶段拥有的全部舰队。

第四关每两秒用 `find_ship_by_true_owner` 重建全部玩家 S/M/L/XL 舰船组，再监听：

- 玩家舰船开始攻击。
- 玩家舰船成为攻击目标。
- 玩家舰船攻击其他对象。
- 玩家舰船受到攻击。

每个事件把战斗窗口延长到 `player.age + 30s`；连续 30 秒无事件才回到平静音乐。

---

## 13. 评分与结果页

### 13.1 只给明确登记的目标计分

所有可计分 Xenon L/XL 创建后加入 `$ScoreXenonCapitalTargets`。`event_object_destroyed` 只监听该组，并要求存在战斗击杀参数。脚本清场没有 killer，不会增加分数。

计分对象与普通敌军组分开，可避免：

- 清理 cue 刷分。
- 中小型舰船误计。
- 无关背景对象误计。

### 13.2 结果细则必须先注册，再打开结果页

稳定顺序：

1. 计算所有子分和最终分。
2. 建立 `ResultData`。
3. 在同一 actions 块调用全部 `add_scenario_stat`。
4. signal 结果 cue。
5. 结果 cue 延迟 1 秒。
6. 调用 `ScenarioFinished`。

如果把 `add_scenario_stat` 放进结果页延迟 cue，最终总分可能显示，但细则行来不及注册。

---

## 14. 第四关最终验证清单

每次修改第四关后至少检查：

- `gatefall_scenario_04.xml` 能通过 `md.xsd`。
- 所有 XML 能解析。
- 中英文 `899902` 文本 ID 一致。
- Phase 1 三轮袭击为 0/60/120 秒。
- 第一艘雷达船护卫清空后会替换目标。
- 第二艘雷达船护卫清空后能解除无敌。
- M0 原始 10 艘 I 具有炮塔、允许移动和开火。
- 玩家自造海豚会被每分钟扫描到。
- 造船厂/船坞摧毁后下一轮时间戳清零且 pulse 被取消。
- 第二座电站即使超时仍能完成。
- 打捞船、打包船、基地和玩家死亡会立即失败。
- 评分细则在结果页出现。
- 平静/战斗 BGM 不会连续无限重复同一随机样本。

---

## 15. 最值得复用的结论

1. 过场道具和实战目标状态复杂时，直接替换对象。
2. 长期循环使用绝对时间戳轮询，不让完成的 cue 自我重启。
3. 玩家自造资产使用 `trueowner` 动态搜索，不依赖出生组。
4. operational 空间站仍要逐模块应用 loadout、补管理员和无人机。
5. ownerless/noattackresponse 恢复后要清飞行员状态、命令和炮塔模式。
6. 动态创建的关键对象使用创建 flag + 轮询失败条件。
7. 空间站 destroyed 状态用显式布尔量收口。
8. 随机 BGM 池由 MD 逐首调度，不能把随机 sample 当播放器队列。
9. 评分先算、先注册细则，再延迟打开结果页。
